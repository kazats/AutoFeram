python -m venv myenv
source myenv/bin/activate
pip install -r requirements.txt
pip install -U polars-lts-cpu
pip install -U polars-lts-cpu


sim15: can forward agent to github

virtualenv venv
source venv/bin/activate
